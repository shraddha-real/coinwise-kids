{
  "name": "coinwise-kids",
  "version": "1.0.0",
  "description": "Interactive financial literacy education web app for children",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "client": "open index.html",
    "test": "mocha tests/**/*.test.js --timeout 10000",
    "test:api": "mocha tests/api/*.test.js --timeout 10000",
    "test:integration": "mocha tests/integration/*.test.js --timeout 10000",
    "test:watch": "mocha tests/**/*.test.js --timeout 10000 --watch",
    "test:coverage": "nyc mocha tests/**/*.test.js --timeout 10000"
  },
  "keywords": [
    "education",
    "finance",
    "kids",
    "learning"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "axios": "^1.10.0",
    "chai": "^4.3.10",
    "chai-http": "^5.1.2",
    "mocha": "^10.2.0",
    "nodemon": "^3.0.1",
    "sinon": "^17.0.1",
    "supertest": "^6.3.3"
  }
}
